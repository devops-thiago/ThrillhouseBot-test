{
  "name": "spend-allocator",
  "version": "1.0.0",
  "private": true,
  "description": "Attributes cloud cost rows to owning teams and renders a monthly chargeback report.",
  "main": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test",
    "build": "node -e \"require('fs').cpSync('src','dist',{recursive:true})\""
  },
  "engines": {
    "node": ">=20"
  }
}
