[project]
name = "thrillhouse-user-api"
version = "0.1.0"
description = "REST API for user management built with FastAPI and SQLite"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
