# syntax=docker/dockerfile:1

FROM golang:1.24 AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o /out/certwatchd ./cmd/certwatchd

FROM gcr.io/distroless/static-debian12:nonroot
COPY --from=build /out/certwatchd /usr/local/bin/certwatchd
USER nonroot
EXPOSE 8080
ENTRYPOINT ["/usr/local/bin/certwatchd"]
